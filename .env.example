# Configure these browser-safe values in the Vercel project environment.
# Never expose a secret key, service-role key, database password, or management token via VITE_*.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
